---
# tasks file for build
#- name: build app
#  shell: ng build 
- name: build app
  command: /root/.nvm/versions/node/v16.18.1/bin/ng "build"
  environment:
    PATH: "{{ ansible_env.PATH }}:/root/.nvm/versions/node/v16.18.1/bin"
